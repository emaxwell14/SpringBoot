import { Component } from '@angular/core';

import {Note} from'./note';

@Component({
  moduleId: module.id,
  selector: 'note-pad-app',
  templateUrl: 'app.component.html'
})
export class AppComponent {
}
